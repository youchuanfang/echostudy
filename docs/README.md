# EchoStudy 文档

- `RUN.md`：环境、数据库、后端、前端启动步骤。
- `API.md`：主要 REST API 路径与用途。

建议演示顺序：

1. 使用 admin 登录后台，查看自习室、座位、时间节点。
2. 使用 student 登录，选择日期、房间、时间节点，通过座位网格预约。
3. 在“我的预约”中演示定位签到、暂离、返座、结束。
4. 使用管理员线下代预约，验证自由时间段和座位冲突校验。
5. 创建 AI 自动预约任务，等待定时任务执行后查看结果。
